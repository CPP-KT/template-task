name: C++ CI

on:
  pull_request

jobs:
  formatting-check:
    name: Formatting check
    runs-on: [self-hosted, ubuntu]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check line breaks
        run: |
          ci-extra/check-lines.sh

      - name: Run clang-format style check
        # TODO use native clang-format
        uses: jidicula/clang-format-action@v4.14.0
        with:
          clang-format-version: '19'
          check-path: '.'

  test:
    needs: formatting-check
    name: "Test (${{ matrix.toolchain.name }}, ${{ matrix.build-type }})"

    strategy:
      fail-fast: false
      matrix:
        toolchain:
          - { name: Linux/GCC,   compiler: gcc-14 }
          - { name: Linux/Clang, compiler: clang-19 }
        build-type:
          - Release
          - Debug
          - RelWithDebInfo
          - Sanitized
          - SanitizedDebug

    runs-on: [self-hosted, ubuntu, "${{ matrix.toolchain.compiler }}"]

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup environment variables
        uses: cardinalby/export-env-action@v2
        with:
          envFile: .github/constants.env

      - name: Update tests
        run: |
          ci-extra/update-tests.sh

      - name: Build
        run: |
          ci-extra/build.sh ${{ matrix.build-type }}
        env:
          CMAKE_TOOLCHAIN_FILE: "/opt/vcpkg/scripts/buildsystems/vcpkg.cmake"

      - name: Run tests
        run: |
          if [[ $TESTS_UPDATED -eq 1 ]]; then
            echo -e '\e[0;33mWARNING: Running a newer version of the tests than you have pushed.\e[0m'
          fi
          ci-extra/test.sh ${{ matrix.build-type }}

      - name: Run tests with valgrind
        if: ${{ matrix.build-type == 'RelWithDebInfo' }}
        run: |
          ci-extra/test-valgrind.sh ${{ matrix.build-type }}
